<%= simple_form_for [ restaurant, review ], remote: true do |f| %>
  <%= f.input :content, label: false, placeholder: "Write your review for #{@restaurant.name}" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
